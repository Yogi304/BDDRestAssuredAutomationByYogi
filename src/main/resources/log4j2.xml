<?xml version="1.0" encoding="UTF-8"?>
 <!-- If Log4j2 is misconfigured (like missing appenders or bad XML), it may fail silently unless its own internal logging is turned on. The status level determines how much info Log4j2 prints about its own behavior to the console during startup. -->
       
<Configuration status="WARN">
    
    <Appenders>
        <Console name="console" target="SYSTEM_OUT">
        	<!--<PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>  -->

            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5level - %msg%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Root logger applies globally to all classes -->
        <Root level="trace">
            <AppenderRef ref="console" />
        </Root>
    </Loggers>

</Configuration>
